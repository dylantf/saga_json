#@ Demos for saga_json. These modules exercise the surviving combinator path:
#@ write a small encoder function that produces `Json`, and a decoder
#@ function built from `SagaJson.Decode`.
module Main

import DecodeHand
import EncodeHand

main () = {
  EncodeHand.run ()
  DecodeHand.run ()
}

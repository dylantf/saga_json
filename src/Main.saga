#@ Demos for saga_json
#@
#@ EncodeDeriveCustom: deriving (ToJson) + the JsonOptions effect for
#@ renaming, tag format, omit-nothings, etc.
module Main

import EncodeDeriveCustom

main () = {
  EncodeDeriveCustom.run ()
}
